<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <a class="navbar-brand" href="/users/dashboard">Onlilne Bus Reservation</a>
      <% elsif agency_signed_in? %>
        <a class="navbar-brand" href="/agencies/dashboard">Onlilne Bus Reservation</a>
      <% else %>
        <a class="navbar-brand" href="/">Onlilne Bus Reservation</a>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
        <% if current_user.present? %>
          <li><%= link_to "#{current_user.email}", users_dashboard_path %></li>
          <li> <%= link_to "Sign out", destroy_user_session_path %></li>
        <% elsif agency_signed_in?%>
          <li><%= link_to "#{current_agency.email}", agencies_dashboard_path %></li>
          <li> <%= link_to "Sign out", destroy_agency_session_path %></li>
        <% else %>
          <li><%= link_to "Sign in", welcome_sign_in_path %></li>
          <li><%= link_to "Sign up", welcome_sign_up_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>